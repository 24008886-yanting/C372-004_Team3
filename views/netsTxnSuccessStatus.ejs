<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment Successful</title>
  <style>
    :root {
      --bg: #f3f8ff;
      --card: #ffffff;
      --ink: #0f172a;
      --muted: #64748b;
      --success: #10b981;
      --success-soft: #d1fae5;
      --border: #e5e7eb;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      min-height: 100vh;
      font-family: "Space Grotesk", "Trebuchet MS", sans-serif;
      background:
        radial-gradient(920px 520px at 0% -10%, #e0f2fe 0%, transparent 60%),
        radial-gradient(720px 500px at 110% 0%, #dcfce7 0%, transparent 55%),
        var(--bg);
      color: var(--ink);
      display: grid;
      place-items: center;
      padding: 32px 16px;
    }
    .card {
      width: min(560px, 100%);
      background: var(--card);
      border-radius: 20px;
      border: 1px solid var(--border);
      box-shadow: 0 16px 40px rgba(15, 23, 42, 0.12);
      padding: 28px;
      text-align: center;
    }
    .badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 6px 14px;
      border-radius: 999px;
      background: var(--success-soft);
      color: var(--success);
      font-weight: 600;
      font-size: 0.9rem;
    }
    .title {
      margin: 14px 0 6px;
      font-size: 1.7rem;
      letter-spacing: -0.02em;
    }
    .subtitle {
      margin: 0 0 18px;
      color: var(--muted);
    }
    .check {
      width: min(140px, 50%);
      margin: 12px auto 18px;
      display: block;
      background: var(--success-soft);
      border-radius: 999px;
      padding: 18px;
    }
    .cta {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 10px 18px;
      border-radius: 12px;
      text-decoration: none;
      color: white;
      background: var(--success);
      font-weight: 600;
      transition: transform 0.15s ease, box-shadow 0.15s ease;
      box-shadow: 0 10px 20px rgba(16, 185, 129, 0.25);
    }
    .cta:hover { transform: translateY(-1px); }
  </style>
</head>
<body>
  <div class="card">
    <div class="badge">Payment successful</div>
    <h1 class="title">Payment Successful!</h1>
    <p class="subtitle"><%= message %></p>
    <img class="check" src="/images/greenTick.png" alt="Payment successful" />
    <a href="/invoice-confirmation" class="cta">View Invoice</a>
  </div>
</body>
</html>
