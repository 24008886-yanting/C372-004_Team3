<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Account Details | Repawblic Pets</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Manrope:wght@500;600;700&family=Space+Grotesk:wght@500;700&display=swap">
  <link rel="stylesheet" href="/css/accountDetails.css">
</head>
<body class="account-body">
  <%- include('partials/navbar') %>

  <main class="account-shell">
    <header class="account-header">
      <div>
        <p class="eyebrow">Account</p>
        <h1>Account Details</h1>
        <p class="subhead">Update your username, contact, email, or reset your password. Changes save directly to your account.</p>
      </div>
      <a class="account-back" href="/profile"><span aria-hidden="true">&#60;</span> Back to Profile</a>
    </header>

    <section class="account-grid">
      <div class="account-card">
        <div class="card-heading">
          <div>
            <h2>Your information</h2>
            <p class="muted">Make updates anytime. Password is optional to change.</p>
          </div>
          <% if (user && user.username) { %>
            <span class="pill">Signed in</span>
          <% } else { %>
            <span class="pill pill--warn">No account loaded</span>
          <% } %>
        </div>

        <% if (success) { %>
          <div class="alert alert--success"><%= success %></div>
        <% } %>
        <% if (error) { %>
          <div class="alert alert--error"><%= error %></div>
        <% } %>

        <form class="account-form" action="/accountDetails" method="POST">
          <% if (user && user.user_id) { %>
            <input type="hidden" name="user_id" value="<%= user.user_id %>">
          <% } %>

          <label class="field">
            <span>Username</span>
            <input type="text" name="username" required placeholder="Your username" value="<%= user && user.username ? user.username : '' %>">
          </label>

          <label class="field">
            <span>Email</span>
            <input type="email" name="email" required placeholder="you@example.com" value="<%= user && user.email ? user.email : '' %>">
          </label>

          <label class="field">
            <span>Contact number</span>
            <input type="tel" name="phone" required placeholder="+65 8123 4567" value="<%= user && user.phone ? user.phone : '' %>">
          </label>

          <label class="field field--full">
            <span>Password</span>
            <input type="password" name="password" placeholder="Leave blank to keep your current password">
          </label>

          <div class="form-actions">
            <button type="submit">Save changes</button>
            <p class="muted">Need help? Reach us via the <a class="link-inline" href="/contact">contact form</a>.</p>
          </div>
        </form>
      </div>

      <aside class="account-side">
        <div class="side-card">
          <h3>Security reminders</h3>
          <ul>
            <li>Strong passwords work best with 12+ characters.</li>
            <li>Use a mix of words, numbers, and symbols.</li>
            <li>Contact details help us keep your orders updated.</li>
            <li>Changes are written directly to your account record.</li>
          </ul>
        </div>

        <% if (user) { %>
          <div class="side-card meta">
            <p class="muted">User ID</p>
            <p class="meta-value"><%= user.user_id %></p>
            <% if (user.email) { %>
              <p class="muted" style="margin-top:8px;">Email on file</p>
              <p class="meta-value"><%= user.email %></p>
            <% } %>
          </div>
        <% } %>
      </aside>
    </section>
  </main>
</body>
</html>
