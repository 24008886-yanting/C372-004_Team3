<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Write a Review</title>
  <link rel="stylesheet" href="/css/product.css">
</head>
<body>
  <%- include('partials/navbar') %>

  <main>
    <section class="card">
      <h1>Review: <%= order_item.product_name %></h1>
      <% if (error) { %><p class="error"><%= error %></p><% } %>

      <form method="POST" action="/reviews">
        <input type="hidden" name="order_item_id" value="<%= order_item.order_item_id %>">
        <input type="hidden" name="product_id" value="<%= order_item.product_id %>">
        <input type="hidden" name="order_id" value="<%= order_item.order_id %>">

        <label>Rating (1-5):</label>
        <select name="rating" required>
          <option value="">Select...</option>
          <option value="5">5 - Excellent</option>
          <option value="4">4 - Good</option>
          <option value="3">3 - OK</option>
          <option value="2">2 - Poor</option>
          <option value="1">1 - Terrible</option>
        </select>

        <label>Comment (optional):</label>
        <textarea name="comment" rows="4"></textarea>

        <button type="submit">Submit review</button>
      </form>

      <a href="/allTransactions">Back to transactions</a>
    </section>
  </main>
</body>
</html>