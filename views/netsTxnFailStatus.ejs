<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Transaction Failed</title>
  <style>
    :root {
      --bg: #fff7f7;
      --card: #ffffff;
      --ink: #1f2937;
      --muted: #6b7280;
      --danger: #dc2626;
      --danger-soft: #fee2e2;
      --border: #f1f5f9;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      min-height: 100vh;
      font-family: "Space Grotesk", "Trebuchet MS", sans-serif;
      color: var(--ink);
      background:
        radial-gradient(900px 500px at 0% -10%, #ffe4e6 0%, transparent 60%),
        radial-gradient(700px 500px at 110% 0%, #fff7ed 0%, transparent 55%),
        var(--bg);
      display: grid;
      place-items: center;
      padding: 32px 16px;
    }
    .card {
      width: min(560px, 100%);
      background: var(--card);
      border-radius: 20px;
      border: 1px solid var(--border);
      box-shadow: 0 16px 40px rgba(31, 41, 55, 0.12);
      padding: 28px;
      text-align: center;
    }
    .badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 6px 14px;
      border-radius: 999px;
      background: var(--danger-soft);
      color: var(--danger);
      font-weight: 600;
      font-size: 0.9rem;
    }
    .title {
      margin: 14px 0 6px;
      font-size: 1.7rem;
      letter-spacing: -0.02em;
    }
    .subtitle {
      margin: 0 0 18px;
      color: var(--muted);
    }
    .cross {
      width: min(180px, 60%);
      margin: 12px auto 18px;
      display: block;
    }
    .cta {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 10px 16px;
      border-radius: 12px;
      text-decoration: none;
      color: white;
      background: var(--danger);
      font-weight: 600;
      transition: transform 0.15s ease, box-shadow 0.15s ease;
      box-shadow: 0 10px 20px rgba(220, 38, 38, 0.25);
    }
    .cta:hover { transform: translateY(-1px); }
    .hint { margin-top: 12px; font-size: 0.9rem; color: var(--muted); }
  </style>
</head>
<body>
  <div class="card">
    <div class="badge">Payment failed</div>
    <h1 class="title"><%= message %></h1>
    <p class="subtitle">Please try again or choose another payment method.</p>
    <img class="cross" src="../images/redCross.png" alt="Transaction failed" />
    <% if (orderId) { %>
      <a class="cta" href="/payment/<%= orderId %>">Return to payment</a>
      <p class="hint">Your order is still saved in cart.</p>
    <% } else { %>
      <a class="cta" href="/purchases">Back to purchases</a>
      <p class="hint">You can retry anytime from your orders.</p>
    <% } %>
  </div>
</body>
</html>
