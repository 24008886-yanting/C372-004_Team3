<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .fail-container {
            background: white;
            border-radius: 12px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.2);
            padding: 60px 40px;
            max-width: 500px;
            width: 100%;
            text-align: center;
        }
        .fail-icon {
            font-size: 80px;
            margin-bottom: 20px;
        }
        .fail-title {
            color: #ef4444;
            font-weight: 700;
            font-size: 28px;
            margin-bottom: 15px;
        }
        .fail-message {
            color: #666;
            font-size: 16px;
            margin-bottom: 30px;
        }
        .retry-btn {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 12px 32px;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            margin-right: 10px;
        }
        .retry-btn:hover {
            text-decoration: none;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
        }
        .back-btn {
            background: #e5e7eb;
            color: #111;
            padding: 12px 32px;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
        }
        .back-btn:hover {
            text-decoration: none;
            color: #111;
            background: #d1d5db;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="fail-container">
            <div class="fail-icon">✕</div>
            <div class="fail-title">Payment Failed</div>
            <p class="fail-message"><%= message %></p>
            <p class="fail-message">Please try again or use another payment method.</p>
            
            <a href="/checkout" class="retry-btn">Retry Payment</a>
            <a href="/cart" class="back-btn">Back to Cart</a>
        </div>
    </div>
</body>
</html>
