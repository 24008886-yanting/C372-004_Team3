<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shelter List | Repawblic Pets</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Manrope:wght@500;600;700&display=swap">
  <link rel="stylesheet" href="/css/shelter.css">
</head>
<body>
  <%- include('partials/navbar') %>

  <main>
    <header>
      <p class="muted">Admin • Shelter Management</p>
      <h1>Partner Shelters</h1>
      <p class="muted">Directory of all current partnering shelters</p>
    </header>

    <% if (currentRole !== 'admin') { %>
      <section class="card">
        <p class="muted">Access restricted. Admins only</p>
      </section>
    <% } else { %>
      <% if (error) { %>
        <section class="card">
          <div class="alert error"><%= error %></div>
        </section>
      <% } %>

      <section class="card">
        <div class="form-actions" style="justify-content: space-between; align-items: center; flex-wrap: wrap;">
          <span class="muted">Total shelters: <%= shelters && shelters.length ? shelters.length : 0 %></span>
          <div>
            <a class="btn outline" href="/shelter">Back</a>
            <a class="btn" href="/addShelter">Add Shelter</a>
          </div>
        </div>

        <% if (!shelters || !shelters.length) { %>
          <p class="muted" style="margin-top:12px;">No shelters found. Add the first partner shelter to get started</p>
        <% } else { %>
          <div class="grid" style="margin-top:16px;">
            <% shelters.forEach((shelter) => { %>
              <article class="card">
                <h2><%= shelter.shelter_name %></h2>
                <p class="muted">Contact: <%= shelter.contact_number || 'N/A' %></p>

                <form action="/shelter/<%= shelter.shelter_id %>/update" method="POST" class="form-grid" style="margin-top:10px;">
                  <div class="form-group">
                    <label for="shelter_name_<%= shelter.shelter_id %>">Shelter Name</label>
                    <input
                      type="text"
                      id="shelter_name_<%= shelter.shelter_id %>"
                      name="shelter_name"
                      value="<%= shelter.shelter_name %>"
                      required
                    >
                  </div>
                  <div class="form-group">
                    <label for="contact_number_<%= shelter.shelter_id %>">Contact Number</label>
                    <input
                      type="text"
                      id="contact_number_<%= shelter.shelter_id %>"
                      name="contact_number"
                      value="<%= shelter.contact_number || '' %>"
                      required
                    >
                  </div>
                  <div class="form-actions" style="flex-wrap: wrap;">
                    <button type="submit" class="btn">Update</button>
                    <button
                      type="submit"
                      class="btn outline"
                      formaction="/shelter/<%= shelter.shelter_id %>/delete"
                      formmethod="POST"
                      onclick="return confirm('Delete this shelter?');"
                    >
                      Delete
                    </button>
                  </div>
                </form>
              </article>
            <% }) %>
          </div>
        <% } %>
      </section>
    <% } %>
  </main>
</body>
</html>
